name: torchrl

channels:
  - pytorch
  - conda-forge

dependencies:
  - python>=3.5
  - pytorch>=0.4
  - numpy
  - scipy
  - opencv
  - pip
  - pip:
      - pyyaml
      - sphinx
      - 'gym[atari]'
      - pandas
      - tensorflow
      - tensorboardX
