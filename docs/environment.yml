name: reward

channels:
  - pytorch

dependencies:
  - python>=3.5
  - pytorch>=0.4
  - numpy
  - scipy
  - pip
  - pip:
      - opencv-python-headless
      - pyyaml
      - sphinx
      - gym
      - pandas
      - tensorflow
      - tensorboardX
